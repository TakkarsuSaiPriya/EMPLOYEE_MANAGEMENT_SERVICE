spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employeedb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  artemis:
    mode: embedded
    embedded:
      enabled: true

server:
  port: 8081
